# Core
streamlit>=1.30.0

# Hugging Face
transformers>=4.40.0
torch>=2.0.0
huggingface_hub>=0.20.0
tokenizers>=0.15.0

# Image processing
Pillow>=10.0.0

# Text-to-Speech (gTTS uses Google's public TTS API — no API key required)
gTTS>=2.5.0

# Optional: Faster BLIP inference (snappier image captioning)
# timm>=0.9.0
